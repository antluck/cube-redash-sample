# ── AI features: choose ONE of the following ──

# Option A: Anthropic API key (recommended — works on any OS)
#   Get one at https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Option B: Claude OAuth token (requires Claude Max/Pro subscription)
#   Run `claude setup-token` to generate, or `./scripts/refresh-token.sh` to auto-extract.
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...
